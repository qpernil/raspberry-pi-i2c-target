name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  rust-and-overlays:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install --yes device-tree-compiler libsdl2-dev
      - name: Install Rust components
        run: rustup toolchain install stable --profile minimal --component rustfmt,clippy
      - name: Select Rust stable
        run: rustup default stable
      - name: Check formatting
        run: cargo fmt --check
      - name: Test
        run: cargo test --locked --all-targets
      - name: Clippy
        run: cargo clippy --locked --all-targets -- -D warnings
      - name: Verify prebuilt Rust checksums
        run: cd prebuilt/aarch64 && sha256sum --check SHA256SUMS
      - name: Compile overlays
        run: make -C kernel overlays
